version: archive-all-email-recs
description: 'archives and removes all email recommendations'

steps:
    - run: |
        chmod u+x ./patch/archive-all-email-recs.sh
        ./patch/archive-all-email-recs.sh